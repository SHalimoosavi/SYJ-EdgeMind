# Windows Platform Layer

**Status: not yet implemented — planned for Phase 5 (Windows Packaging), after Phase 1's core runtime and CLI exist.**

This directory will hold Windows-specific build notes, packaging conventions, and troubleshooting once `scripts/build-windows.ps1` / `build-windows.bat` and the CLI target land. It will not contain inference logic — it wraps `src/api/edge_mind_api.h`, the single shared C API.
