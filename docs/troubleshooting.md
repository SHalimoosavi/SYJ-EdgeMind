# Troubleshooting

**Status: Phase 0 — no runtime exists yet, so there is nothing to troubleshoot in practice.** This page will be populated with real, encountered issues starting in Phase 1 (build/runtime errors) and Phase 5 (Windows packaging).

Planned structure once populated:

- Build failures (CMake / MSVC / toolchain)
- Model load failures (checksum mismatch, unsupported format, insufficient memory)
- Unsafe-configuration errors and how to resolve them (reduce context, lower memory budget, choose a smaller model)
- CPU feature-detection issues
- Platform-specific notes (Windows / iOS)

Entries here must reflect real, reproduced issues — not hypothetical ones.
