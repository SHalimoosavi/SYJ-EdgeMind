# Roadmap

SYJ EdgeMind is built one phase at a time. A phase is not started until the previous phase's acceptance criterion is met. Status is updated at the end of each phase.

| Phase | Name | Acceptance Criterion | Status |
|---|---|---|---|
| 0 | Architecture & Repository Bootstrap | Repo skeleton, docs skeleton, CMake foundation, and dependency strategy exist | **Done** |
| 1 | Native llama.cpp Runtime | A local GGUF model can be loaded and can generate text | **Done — confirmed on real hardware (Android/Termux, SmolLM2-135M-Instruct-Q4_K_M.gguf) by the project maintainer; commit `230fccc`, tag `v0.1.1`** |
| 2 | Memory Safety Engine | Runtime refuses unsafe configurations rather than crashing or exhausting RAM | **Implemented, including a fail-closed estimation design (invalid/unavailable hyperparameters reject the configuration, never default to a 0-byte estimate) and 3 corrections applied after initial review — see docs/development.md's "Corrections applied" section. Pure estimator/policy components build-verified and unit-tested for real in this sandbox (5/5 tests pass); llama.cpp-integration points (memory_observer, InferenceEngine wiring) syntax-checked only — not yet confirmed against real llama.cpp/real hardware.** |
| — | **Usage/Quota Manager Foundation (v0.3.0, parallel initiative)** | **A user-configured local usage limit (session time / daily messages / daily tokens) is enforced, persists across restarts, and fails closed on corrupted state** | **Implemented — see docs/usage-model.md. Note: this row was not part of the original phase-numbered plan below; it was introduced by explicit request as parallel work alongside (not instead of) Phase 3. Flagging this discrepancy rather than silently renumbering the existing phases — see docs/development.md's v0.3.0 audit note. 8/8 pure/filesystem tests build-verified and run for real in this sandbox; Runtime/CLI/C API integration syntax-checked only.** |
| 3 | Model Registry & Verification | A user can safely acquire/import a GGUF model and verify it before inference | **Implemented — see docs/model-registry.md. GGUF structural validation (`GgufReader`) and model identity (`Sha256`) are deliberately independent of llama.cpp, built directly against the public GGUF spec; `ModelVerifier`/`ModelRegistry` compose those with filesystem checks and local persistence; `Runtime::load()` gates on verification before memory admission or `InferenceEngine::load()` is ever reached — an unverified/malformed file never reaches `llama_model_load_from_file()`. 4/4 new test binaries (test_gguf_reader, test_model_hash, test_model_verifier, test_model_registry) build-verified and run for real in this sandbox against real fixtures (including two adversarial "absurd declared length" cases) and real NIST/FIPS SHA-256 vectors. Runtime/CLI/C API integration syntax-checked only. Real Termux `cmake`/`ctest` hardware validation is pending — no cmake or network access exists in this sandbox (same limitation as every prior phase) — see docs/development.md's Phase 3 audit note.** |
| — | **Runtime Model Loading & Inference Integration (v0.5.0, parallel initiative)** | **A verified/registered model can be resolved by registry identity (not only by direct path) and cleanly loaded/unloaded by the runtime, without bypassing existing memory or usage/quota controls** | **Implemented — see docs/model-registry.md. Not part of the original phase-numbered plan below; introduced by explicit request as parallel work alongside Phase 4, same precedent as v0.3.0 above — flagging rather than silently renumbering. New `model_resolver` module (pure, llama.cpp-independent) resolves `model_path` OR `model_id` (registry lookup) into a path, feeding the exact same unmodified verify-then-register-then-load pipeline either way. `InferenceEngine::unload()`/`Runtime::unload()` added, fixing a real double-load handle leak found during this milestone's own audit (load() previously never freed a prior model before overwriting its handles). New C API: `model_id` config field (append-only; see the ABI-compatibility note in `edge_mind_api.h` for why this is documented as source-compatible but not binary-ABI-stable across versions), `syj_edgemind_abi_version()`, `syj_edgemind_unload()`, `syj_edgemind_list_models()`. CLI: `--model-id`, `--list-models`, `/models`, `/unload`. 6/6 relevant real tests (test_model_resolver's 7 checks, test_config's 3 new checks, plus the full pre-existing suite) build-verified and run for real in this sandbox. Runtime/InferenceEngine/C API/CLI integration syntax-checked only — the unload/reload lifecycle against real llama.cpp is explicitly NOT hardware-validated here; see docs/model-registry.md's "Validation status".** |
| 4 | Production CLI | A technically non-expert user can run local inference with minimal configuration | **Implemented — external Android/Termux validation pending. See docs/model-registry.md. Deterministic 0/1/2+ registry auto-selection (`select_model_for_cli` in `src/core/model_selection.h/.cpp`, deliberately NOT in `src/cli/` — `main.cpp` is architecturally restricted to the C API header only, so this lives alongside `Runtime` and is exposed via a new `syj_edgemind_select_model()` C API function instead): 0 registered models gives actionable guidance, exactly 1 auto-selects with an explicit printed confirmation (never silent), 2+ requires an explicit `--model`/`--model-id` choice and never guesses. Auto-selection feeds the resolved identity through the unmodified v0.5.0 resolution → verification → admission → load pipeline, never a shortcut. New `/context` command surfaces `ContextManager`'s already-tracked state through the existing Runtime → C API → CLI reporting layers, duplicating no accounting logic. 12/12 real test binaries build-verified and run for real in this sandbox (11 from Phase 3/v0.5.0 + `test_model_selection`'s 12 checks), zero regressions. Runtime/CLI/C API integration syntax-checked only. `syj_edgemind_config` was NOT modified (purely additive C API changes, per explicit constraint). The v0.5.0 real-GGUF load→unload→reload lifecycle test remains explicitly PENDING — real Termux 14/14 CTest confirmed llama.cpp links and the existing suite passes, but no test in that suite exercises that exact lifecycle against a real linked model yet; this is not being silently counted as closed.** |
| 5 | Windows Packaging | A clean Windows machine with the required toolchain can build and run the project reproducibly | **Implemented — statically audited; real Windows/MSVC execution NOT performed. See platform/windows/README.md and docs/supported-platforms.md.** `scripts/build-windows.ps1` (authoritative) + `scripts/build-windows.bat` (thin delegator) with explicit Portable (default)/AVX/AVX2 CPU-mode selection — compile-time instruction-set control only, since the pinned ggml build has no runtime CPU-feature dispatch (verified against ggml's real CMake config, not assumed); `GGML_NATIVE` never enabled, AVX/AVX2 always explicit opt-ins, never inferred from the build machine. A narrowly-scoped persistence portability fix was applied to exactly two pre-existing call sites (`ModelRegistry::save()`, `UsageStateStore::save()`) — a `MoveFileExA`/`MOVEFILE_REPLACE_EXISTING`-based replacement operation under `#ifdef _WIN32`, designed to preserve the same atomic-write intent as the existing POSIX path (not claimed as an unconditional filesystem-level atomicity guarantee in every Windows storage configuration), addressing a previously-documented gap (Windows' `std::rename` fails rather than replacing an existing file, unlike POSIX) — the POSIX path is byte-for-byte unchanged and all 5 real tests exercising those two files still pass. `CMakeLists.txt` was not modified — CPU-mode flags are passed by the build scripts at configure time, no new CMake logic was needed. Neither Windows script has been executed or parser-validated (no PowerShell/cmd.exe interpreter available in the sandbox that built this) — this is explicitly not claimed as hardware-confirmed. Two defects found via external pre-release review were fixed: an unreliable exit-code-propagation bug (`Write-Error` under `$ErrorActionPreference = "Stop"` could skip the following `exit` statement — fixed at all three affected sites) and a missing explicit `-A x64` architecture pin (added; no specific Visual Studio version is hardcoded, since only "MSVC" is documented, never a VS release — an optional `-Generator` parameter is available instead). See CHANGELOG.md's "Fixed — Phase 5" entry.** |
| 6 | iOS Native Bridge | The same inference core can be invoked from iOS | Not started |
| 7 | iOS Minimal UI | Minimal SwiftUI app: model selection, chat, stop generation, context reset, memory info, offline indicator | Not started |
| 8 | Performance & 4 GB Optimization | Application behaves predictably under the 4 GB memory target, measured on real hardware | Not started |
| 9 | Testing & Hardening | Full unit/integration/memory/corruption/config/platform test coverage; sanitizers and static analysis run | Not started |
| 10 | Release Candidate (v0.1.0-rc1) | Dependency, license, security, README, build-reproducibility, model-registry, and cross-platform audits all pass | Not started |
| 11 | v0.1.0 | CHANGELOG, release notes, supported-model table, benchmark table, known limitations, release published | Not started |

## Non-negotiable constraints throughout all phases

- One inference core (C/C++ + llama.cpp), no duplicated per-platform inference logic.
- CPU-only by default; no GPU/cloud dependency required.
- Offline after model acquisition; zero telemetry.
- No invented benchmark numbers, model URLs, or API functions.
- No placeholder implementations presented as working code.
